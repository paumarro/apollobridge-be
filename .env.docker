# Application Configuration
PORT=3000
APOLLO_DOMAIN=http://art-service:3000  # Use the service name for internal Docker network communication

# Keycloak Configuration
KEYCLOAK_URL=http://keycloak:8080  # Keycloak service URL within Docker
KEYCLOAK_REALM=apollo
KEYCLOAK_CLIENT_ID=apollo-client
KEYCLOAK_CLIENT_SECRET=JzluNh2qIzhUjbgx6UenaGrdvAFoI9fa
KEYCLOAK_DOMAIN=http://keycloak:8080  # Internal Docker hostname for Keycloak
REDIRECT_URL=http://art-service:3000/auth/callback  # Update based on your service endpoint
JWKS_URL=http://keycloak:8080/realms/apollo/protocol/openid-connect/certs

# Database Configuration
POSTGRES_USER=myuser
POSTGRES_PASSWORD=pass
POSTGRES_DB=art
ART_DB_URL=postgres://myuser:pass@db:5432/art  # Use the 'db' hostname for the database service in Docker

# Keycloak Admin Credentials
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=adminpass

# Test Database Configuration (Optional for Dockerized Testing)
PG_TEST_USER=test_user
PG_TEST_PASSWORD=test_password
PG_TEST_HOST=db  # Use the same hostname as the main database
PG_TEST_PORT=5432
PG_TEST_DB=test_db
TEST_DB_DSN=postgres://test_user:test_password@db:5432/test_db?sslmode=disable

# Network Configuration (if needed for external services)
DOCKER_NETWORK=apollo-net  # Optional, if you need to reference the Docker network
